# whoop-mcp (reference checkout)

Colin's fork of a Whoop MCP server (Whoop API v2 -> Claude connector). This checkout is NOT what runs: the deployed copy lives at ~/ai-os/services/whoop_mcp (docker `ai-os-whoop-mcp-1`, host port 8976). Make production changes there via the ai-os stack; treat this clone as reference only.
